all:
  vars:
    ansible_ssh_private_key_file: ~/.ssh/google_compute_engine

kubernetes_hosts:
  children:
    kubernetes_master:
      hosts:
        tf-controller0.labs.networkreliability.engineering:
    kubernetes_workers:
      hosts:

        # TODO need to do dynamic inventory for these
        tf-compute0:
        tf-compute1:
db_hosts:
  hosts:
    db0.labs.networkreliability.engineering:
