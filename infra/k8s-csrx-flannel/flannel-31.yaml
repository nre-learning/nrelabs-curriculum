apiVersion: "kubernetes.com/v1"
kind: Network
metadata:
  name: flannel-31
plugin: flannel
args: '[
        {
                "type": "flannel",
                "delegate": {
                        "isDefaultGateway": true
                }
        }
]'
